import { BrowserRouter } from "react-router-dom";

import Router from "../../shared/components/Router";

const App = () => (
  <BrowserRouter>
    <Router />
  </BrowserRouter>
);

export default App;
